# IdeaVim fixtures this host does not match yet.
#
# Generated by VimFixtureReplayTest, which writes the current list to
# build/fixture-failures.txt with the difference for each one, and a header saying how many were
# harvested and why the rest were not. To update this file, read that one and copy the names across
# - never edit a name by hand to make the build pass.
#
# 955 of the 966 harvested fixtures pass. These eleven are grouped by what is actually wrong,
# which is six things.
#
# The corpus went 314 -> 642 -> 966 as the parser learned what these tests are written with. The
# last step was `trimMargin`, which 318 of them use instead of `trimIndent`, plus `.repeat(n)`,
# `"a" + "b"` concatenation and `\uXXXX`. "An argument this cannot evaluate" fell from 397 to 26.

# --- `<Action>` and `<Plug>` in a mapping, typed rather than mapped.
#
# All four are about what happens to the *keys* of a half-typed `<Action>` or `<Plug>` mapping,
# which this host replays as literal text instead of recognising.
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test abandoned Action mapping replays all keys as text
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test abandoned Action mapping replays all keys as text 2
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test abandoned Plug mapping replays all keys as text
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test partial Action mapping

# --- Reindenting, which is a VS Code command and therefore asynchronous.
#
# `=` on a sentence object asks the host to reindent a range. VS Code's is a command that resolves a
# promise, and the engine wants the text changed before it returns.
src/test/java/org/jetbrains/plugins/ideavim/action/motion/object/MotionOuterSentenceActionTest.kt:test on empty last line
src/test/java/org/jetbrains/plugins/ideavim/action/motion/object/MotionSentenceActionTest.kt:test indent outer sentence on empty last line

# --- `<expr>` mappings, whose right-hand side is a Vimscript expression.
#
# `:vmap <expr> q '<Esc>a - line(v)=='.line('v').'<Esc>'` types its own source here instead of being
# evaluated.
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/functions/cursorFunctions/LineFunctionTest.kt:test line with reverse selection
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/functions/cursorFunctions/LineFunctionTest.kt:test line with selection

# --- One each.
#
# `ideajoin` is IntelliJ's smart join, a language feature this host cannot have. The other two are
# Select mode corners: block Select entered with `g<C-H>`, and where the caret lands when a search
# is run from Select mode with `<C-O>`.
src/test/java/org/jetbrains/plugins/ideavim/action/change/delete/DeleteJoinVisualLinesSpacesActionTest.kt:test join via idea
src/test/java/org/jetbrains/plugins/ideavim/action/motion/leftright/MotionShiftLeftActionHandlerTest.kt:test multiline with empty line block mode
src/test/java/org/jetbrains/plugins/ideavim/action/motion/search/SearchEntryFwdActionTest.kt:search in one time from select mode
