# IdeaVim fixtures this host does not match yet.
#
# Generated by VimFixtureReplayTest, which writes the current list to
# build/fixture-failures.txt with the difference for each one, and a header saying how many were
# harvested and why the rest were not. To update this file, read that one and copy the names across
# - never edit a name by hand to make the build pass.
#
# 1228 of the 1236 harvested fixtures pass.
#
# The corpus has grown by 189 in two steps, and both were the harness looking at less of `src/test`
# than it thought. First it only found backtick-quoted test names, so IdeaVim's 578 plainly-named
# tests were *invisible* rather than refused - they never reached the skip counts either, so the
# reported yield was measuring what could be parsed out of what could be seen. Then it only read a
# method whose body *started* with `doTest`, so a method that bound its text first, or called
# `doTest` more than once, gave up everything but the first call.
#
# Nine of the 189 failed. Seven were real gaps in this host; the other two were this file being
# read in isolation when the fixtures are not replayed in isolation. `VimSearchGroupBase` keeps
# `lastPatternTrailing` and three of its neighbours in a `protected companion object` - one per
# process, however many search groups exist - so `n` after `:s` inherited the `3` from a `/and/3`
# two fixtures earlier and answered three lines below the match it had correctly found. The replay
# resets the engine between fixtures now, the way IdeaVim's own `VimTestCase.setUp` does. Order
# dependence is worse than any single failure: every fixture was being replayed into whatever its
# neighbour had left behind.
#
# The ten that left it most recently are worth telling apart, because only eight were bugs here.
#
# Eight were blockwise Visual, and one bug: a block's caret goes in the *active corner's* column
# rather than the block's right edge, a line too short to hold the block's slice contributes no
# caret at all, carets that have stopped being different carets merge, and two pieces of per-caret
# state have to outlive the caret that recorded them. `VsCodeBlockVisualTest` has the numbers.
#
# One was the harness harvesting tests IdeaVim has `@Disabled`. `search in one time from select
# mode` sat here as "expected [81], actual [82]" when IdeaVim's own caret is at 82, and `repeat
# command with execution of ij action` had a reason written against it - a missing IntelliJ action -
# that was not why it failed. A disabled test does not pass over there either. Before believing an
# entry in this file, check that the fixture passes in the plugin.
#
# And one was `'incsearch'` previewing an ex command, which this port had recorded as needing "a
# different piece of machinery from previewing `/`". True, and the machinery already existed: it was
# `parseCommandLineForPreview` in `ExEntryPanel`, in `src/main/java` next to a Swing document
# listener, with no IntelliJ in the body of it. It is `incsearchPreviewRequest` in the engine now
# and both hosts read it.

# --- `o` and `O` do not match the indent of the line they open from.
#
# Six fixtures, one gap. `VsCodeInjector`'s `InsertNewLineAction` says so out loud - "Vim's own `o`
# would indent the new line to match; `'autoindent'` is not wired up yet, so this is the newline and
# nothing else" - and these are IdeaVim saying what the indent should be. Nothing about it is
# host-shaped: the indent of a line is a prefix of that line.
src/test/java/org/jetbrains/plugins/ideavim/action/change/insert/InsertNewLineAboveActionTest.kt:test insert new line above matches indent for first line of plain text
src/test/java/org/jetbrains/plugins/ideavim/action/change/insert/InsertNewLineAboveActionTest.kt:test insert new line above matches indent for plain text
src/test/java/org/jetbrains/plugins/ideavim/action/change/insert/InsertNewLineAboveActionTest.kt:test insert new line above with multiple carets
src/test/java/org/jetbrains/plugins/ideavim/action/change/insert/InsertNewLineBelowActionTest.kt:test insert new line below matches indent for plain text
src/test/java/org/jetbrains/plugins/ideavim/action/change/insert/InsertNewLineBelowActionTest.kt:test insert new line below matches indent for plain text 1
src/test/java/org/jetbrains/plugins/ideavim/action/change/insert/InsertNewLineBelowActionTest.kt:test insert new line below with multiple carets

# --- Two IntelliJ actions, which this host has no equivalent of.
#
# `ideajoin` is IntelliJ's smart join, a language feature this host cannot have: `VjJ` with the
# option set joins the way the language wants rather than the way Vim does.
#
# `partial Action mapping` types `X(EditorToggleCase)` into a half-declared `<Action>` mapping. The
# mapping machinery works - nothing is inserted as literal text, which is what this entry used to
# claim - and `EditorToggleCase` is an IntelliJ action id with no VS Code command behind it. There
# is no toggle-case command; `editor.action.transformToUppercase` and its lowercase twin are the
# whole of what VS Code offers.
src/test/java/org/jetbrains/plugins/ideavim/action/change/delete/DeleteJoinVisualLinesSpacesActionTest.kt:test join via idea
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test partial Action mapping
