# IdeaVim fixtures this host does not match yet.
#
# Generated by VimFixtureReplayTest, which writes the current list to
# build/fixture-failures.txt with the difference for each one. To update this file, read that one
# and copy the names across - never edit a name by hand to make the build pass.
#
# 304 of the 314 harvested fixtures pass. These ten are grouped by what is actually wrong, which is
# five things.
#
# Ten of the twenty this list started with have gone, and they were two bugs. Seven were the empty
# line that a trailing newline opens, which this host did not count - so `cc` and `dd` on the last
# line took the range of the line above and `G` stopped short. Three were replace mode: nothing here
# had pressed `R`, so it did not overwrite, and backspace did not put back what it had.

# --- Select mode, which this host has not built at all.
#
# `exitSelectModeNative` is still a TODO. Vim's Select mode is the one where typing replaces the
# selection, and it is reached from Visual mode with `<C-G>` or by IntelliJ's own idea of a
# selection. Four fixtures, one missing method.
src/test/java/org/jetbrains/plugins/ideavim/action/motion/select/motion/SelectMotionLeftActionTest.kt:test char select simple move
src/test/java/org/jetbrains/plugins/ideavim/action/motion/select/motion/SelectMotionRightActionTest.kt:test char select simple move
src/test/java/org/jetbrains/plugins/ideavim/action/motion/updown/MotionArrowDownActionTest.kt:test char select simple move
src/test/java/org/jetbrains/plugins/ideavim/action/motion/updown/MotionArrowUpActionTest.kt:test char select simple move

# --- Reindenting, which is a VS Code command and therefore asynchronous.
#
# `=` on a sentence object asks the host to reindent a range. VS Code's is a command that resolves a
# promise, and the engine wants the text changed before it returns.
src/test/java/org/jetbrains/plugins/ideavim/action/motion/object/MotionOuterSentenceActionTest.kt:test on empty last line
src/test/java/org/jetbrains/plugins/ideavim/action/motion/object/MotionSentenceActionTest.kt:test indent outer sentence on empty last line

# --- `<Action>` in a mapping, typed rather than mapped.
#
# Both are about what happens to the *keys* of a half-typed `<Action>` mapping, which this host
# replays as literal text instead of recognising.
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test abandoned Action mapping replays all keys as text 2
src/test/java/org/jetbrains/plugins/ideavim/ex/implementation/commands/MapCommandTest.kt:test partial Action mapping

# --- One each.
#
# `ideajoin` is IntelliJ's smart join, which is a language feature this host cannot have. `yVl` is a
# forced-motion case that reaches something the engine itself has not finished.
src/test/java/org/jetbrains/plugins/ideavim/action/change/delete/DeleteJoinVisualLinesSpacesActionTest.kt:test join via idea
src/test/java/org/jetbrains/plugins/ideavim/action/change/ForcedMotionVModifierTest.kt:test yVl yanks the whole line linewise and pastes below
