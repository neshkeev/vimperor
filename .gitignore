*.swp
/.gradle/
/.intellijPlatform/

/.idea/
!/.idea/dictionaries/project.xml
!/.idea/scopes
!/.idea/copyright
!/.idea/icon.png
!/.idea/inspectionProfiles
!/.idea/fileTemplates
!/.idea/runConfigurations
!/.idea/codeStyles
!/.idea/vcs.xml
!/.idea/misc.xml
!/.idea/.name
!/.idea/gradle.xml

**/build/
**/out/
**/tmp/

# The VS Code extension's packaging output: `dist/` is a copy of the production bundle that
# `assembleExtension` makes, and the `.vsix` is built from it. Both are generated, and `dist/`
# is not under `build/` precisely so that `vsce` can reach it.
/vscode-extension/dist/
*.vsix

*.DS_Store

.teamcity/.idea
.teamcity/target
.teamcity/*.iml

# Created by github automation
settings.xml

.kotlin

.claude/settings.local.json

.beads/sync_base.jsonl

# Split-mode test artifacts
**/allure-results/
